{
  "name": "budget-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker": "npm run docker-build && npm run docker-run",
    "docker-build": "docker build -t georgeflug/budget-backend .",
    "docker-run": "docker run -p3000:3000 georgeflug/budget-backend",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Richard White",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  }
}
